---
- block:
  - name: Stop server
    command: /etc/init.d/dachs stop

  - name: Setup Dachs server
    template:
      src: gavo-service.j2
      dest: /etc/systemd/system/gavo.service

  - name: Start server
    service:
      name: gavo
      state: started
      enabled: yes
      daemon_reload: yes
  become: yes
  become_method: sudo
  become_user: root